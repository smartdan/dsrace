    1  htop
    2  /DietPi/dietpi/finalise 
    3  ls -lha /home
    4  userdel -f dietpi
    5  ls -lha home
    6  ls -lha /home
    7  rm -R home
    8  rm -R /home
    9  ls -lha
   10  df -h
   11  dpkg -l
   12  apt-get purge klibc-utils
   13  dpkg -l | grep initramfs-tools
   14  ls -lha
   15  poweroff
   16  dietpi-config
   17  ls
   18  startx
   19  pi
   20  logout

   21  sudo cat /dev/ttyUSB0
   22  sudo cat /dev/ttyCAT0
   23  sudo cat /dev/ttyCAT0
   24  sudo cat /dev/ttyACM0

sudo apt-get install gpsd gpsd-clients python-gps
sudo apt-get install arrow chronograph input 
sudo pip install arrow chronograph input 
pip install geopy
sudo pip install arrow chronograph input 
sudo pip install telepot
sudo pip install shapely
sudo apt-get install libgeos++
sudo systemctl stop gpsd.socket
sudo systemctl disable gpsd.socket
sudo pip install shapely

sudo gpsd /dev/ttyACM0 -F /var/run/gpsd.sock
cgps -s


sudo curl -sS get.pimoroni.com/displayotron | bash
curl -sS get.pimoroni.com/displayotron | bash

   41  mkdir /home; mkdir /home/pi
   42  cd
   43  ls
   44  cd ..
   45  ls
   46  mkdir /home/pi
   47  useradd --group 100 --uid 1000 --shell /bin/bash --home /home/pi pi
   48  useradd --group 100 --uid 1001 --shell /bin/bash --home /home/pi pi
   49  sudo apt-get install sudo
   50  chmod 4755 /usr/bin/sudo 
   51  usermod -G sudo pi
   52  passwd pi
   53  chown pi:users /home/pi
   54  sudo crontab -e
   55  logout
   56  exit
   57  dietpi-software
   58  dietpi-launcher
   59  dietpi-config
   60  quit
   61  exit

   62  sudo nano /etc/rc.local 
   63  ps aux | grep py
   64  ps aux | grep .py
   65  ps aux
   66  dietpi-config
   67  exit
   68  logout
   69  exit
   70  logout
   71  login
   72  login
   73  sudo nano /etc/rc.local 
   74  sudo crontab -e
   75  sudo crontab -e
   76  sudo crontab -e
   77  sudo reboot
   78  ps aux | grep .py
   79  sudo nano /etc/rc.local 
   80  sudo halt
   81  exit
   82  cpu
   83  sudo hslt
   84  sudo halt
   85  ls
   86  cd /
   87  ls
   88  sudo  nano cdDietPi/c
   89  sudo  nano DietPi/config.txt 
   90  sudo  nano DietPi/config.txt 
   91  sudo reboot
   92  df
   93  ls /home/
   94  hostname -I
   95  ps aux | grep vnc
   96  ls
   97  login
   98  sudo apt-get install gedit
   99  ls /home/clear
  100  clear
  101  df
  102  ps aux | grep vnc
  103  ls
  104  ls /home/
  105  ls /home/pi/
  106  sudo apt-get update
  107  sudo apt-get upgrade
  108  sudo apt-get autoremove
  109  sudo apt-get upgrade
  110  sudo apt-get remove chromium-browser*
  111  sudo apt-get remove chromium-browser
  112  sudo apt-get purge chromium-browser
  113  hostname -I
  114  sudo reboot
  115  sudo nano /etc/rc.local 
  116  sudo crontab -e
  117  sudo dietpi-update
  118  sudo reboot
  119  history | history.txt
  120  history >  history.txt


----
Start with lite and then run
sudo apt-get install --no-install-recommends gvfs raspberrypi-ui-mods xserver-xorg-video-fbturbo lxterminal
That should get you the pixel desktop and the essentials without going over 4GB